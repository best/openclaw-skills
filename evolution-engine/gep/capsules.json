{
  "version": 1,
  "capsules": [
    {
      "id": "capsule_subagent_delivery_fix",
      "name": "Fix Subagent Message Delivery in Telegram Context",
      "trigger": "Subagent spawned from cron/heartbeat fails to deliver messages to Telegram",
      "steps": [
        "Identify: grep gateway log for 'Unknown target' or 'requires target <chatId>'",
        "Root cause: subagent lacks implicit chat context — cron runs have no originating chatId",
        "Fix: use sessions_spawn (auto-announces to requester) instead of message tool with target",
        "If direct messaging needed: use sessions_send to relay through parent session",
        "Never use target='main' — Telegram requires numeric chatId"
      ],
      "gene": "gene_subagent_telegram_target",
      "origin": "PCEC #12 — 2026-02-22, abstracted from 5+ gateway errors"
    },
    {
      "id": "capsule_chronic_announce_retry",
      "name": "Diagnose Chronic Subagent Announce Retry Loop",
      "trigger": "Same runId appears in 'Subagent completion direct announce failed' 5+ times across 3+ hours",
      "steps": [
        "Extract runId: grep 'Subagent completion direct announce failed' gateway log | extract run UUID",
        "Count occurrences and time span to confirm chronic (not transient burst)",
        "Root cause: gateway retry loop — run completed but announce target unreachable, retries indefinitely",
        "This is a gateway-level issue, not fixable from agent side",
        "Action: log as known pattern, do not restart gateway (won't help), monitor for resolution on next gateway update"
      ],
      "gene": "gene_subagent_announce_timeout",
      "origin": "PCEC #13 — 2026-02-22, run bf97824f retried 14 times over 12 hours"
    },
    {
      "id": "capsule_llm_cascade_recovery",
      "name": "Diagnose and Recover from LLM Provider Cascade Failure",
      "trigger": "Multiple lanes fail simultaneously with timeout + 502; 'All models failed' in gateway log",
      "steps": [
        "Detect: grep gateway log for 'All models failed' or count simultaneous 'LLM request timed out' across lanes",
        "Verify cascade: check if failures span main + cron + session lanes within a 15-min window",
        "Root cause: all models route through same upstream (e.g., deeprouter → Anthropic); single point of failure",
        "Immediate: no action needed — system auto-recovers when upstream returns; expect queued cron batch-fire",
        "Post-recovery: check for duplicate cron deliveries from batch-fire (multiple PCEC triggers arriving together)",
        "Prevention: configure at least one fallback model on a different upstream provider for critical cron jobs (e.g., MiniMax M2.5 as cron fallback)"
      ],
      "gene": "gene_llm_provider_cascade_failure",
      "origin": "PCEC #14 — 2026-02-22, 12-min total outage caused 3 PCEC triggers to batch"
    },
    {
      "id": "capsule_inter_session_announce_routing",
      "name": "Correctly Route Inter-Session Messages to User",
      "trigger": "Subagent/dev-mgr reports progress via sessions_send; user needs to be notified",
      "steps": [
        "Receive sessions_send from subagent (e.g., dev-mgr reports task complete)",
        "Evaluate: is this worth forwarding to the user? (task complete, error, important progress → yes; routine ack → no)",
        "DO NOT reply conversationally to the subagent — it's an AI, not a human",
        "To notify user: use the message tool to send to Telegram directly (主 session 有 chat context，直接发就行)",
        "DO NOT use [[reply_to_current]] — in inter-session context this replies to the sending session, not Telegram",
        "Keep subagent replies minimal and directive (next task instruction or acknowledgment), never emotional/social"
      ],
      "gene": "gene_subagent_telegram_target",
      "origin": "PCEC #17 — 2026-02-23, extracted from 2026-02-22 memory lesson about dev-mgr notification failure"
    }
  ]
}